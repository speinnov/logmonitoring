Starting the single-node Zookeeper instance:

bin/zookeeper-server-start.sh config/zookeeper.properties
Starting the Kafka Server:

bin/kafka-server-start.sh config/server.properties
Listing the Topics available in Kafka:

bin/kafka-topics.sh --list --zookeeper localhost:2181

bin/kafka-console-consumer.sh --zookeeper localhost:2181 --topic kafkatopic1 --from-beginning

kafka-console-consumer.sh --bootstrap-server localhost:9093 --topic kafkatopic --from-beginning


bin/kafka-console-producer.sh --broker-list localhost:9093 --topic kafkatopic1

> bin/kafka-topics.sh --create --bootstrap-server localhost:9093 --replication-factor 1 --partitions 1 --topic kafkatopic1

kafka-topics.sh --create --zookeeper localhost:2181 --replication-factor 1 --partitions 1 --topic kafkatopic1

$KAFKA_HOME/bin/kafka-server-start.sh $KAFKA_HOME/config/server.1.properties

